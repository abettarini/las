# Esempio di configurazione delle variabili di ambiente
# Copia questo file in .env per lo sviluppo locale o .env.production per la produzione

# URL base dell'API (Cloudflare Worker)
VITE_API_URL=http://localhost:8787