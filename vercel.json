{
  "installCommand": "pnpm install",
  "buildCommand": "npx prisma generate && cd frontend && pnpm run build",
  "outputDirectory": "frontend/dist",
  "rewrites": [
    { "source": "/api(.*)", "destination": "/api/index" },
    { "source": "/(.*)",     "destination": "/index.html" }
  ]
}
